<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Authority Dashboard | CIVIC - AI</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Poppins:wght@400;500;700&display=swap" rel="stylesheet">
    
    <script src="https://cdn.tailwindcss.com"></script>
    
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Inter', 'sans-serif'],
                        poppins: ['Poppins', 'sans-serif'],
                        arial: ['Arial', 'sans-serif']
                    },
                    colors: {
                        civic: {
                            dark: '#102746',
                            bg: '#f9fafb',
                            textGray: '#474747',
                            border: '#d0d5db',
                            
                            // Status Colors
                            pending: '#fee2e2',
                            pendingText: '#ef4444',
                            
                            progressBg: '#dbeafe', 
                            progressText: '#2563eb', 
                        }
                    },
                    backgroundImage: {
                        'header-gradient': 'linear-gradient(180deg, rgba(6, 30, 63, 1) 0%, rgba(6, 30, 63, 0.95) 40%, rgba(39, 96, 172, 0) 100%)',
                    }
                }
            }
        }
    </script>
</head>
<body class="bg-white min-h-screen font-sans pb-20">

    <div class="relative w-full h-[144px]">
        <div class="absolute inset-0 w-full h-full bg-header-gradient backdrop-blur-[18.95px] -ml-1 z-0"></div>
        
        <div class="relative z-10 container mx-auto px-6 h-full flex justify-between items-start pt-8">
            <div class="flex items-center gap-6">
                <img class="w-[87px] h-[78px] object-contain" src="https://c.animaapp.com/LNwVnAWI/img/chatgpt-image-jan-2--2026--11-51-48-pm-1@2x.png" alt="Logo">
                <div class="text-white">
                    <h1 class="font-bold text-2xl tracking-wide">CIVIC - AI</h1>
                    <p class="font-poppins text-base opacity-90 tracking-wide">Issue Detection & Resolution Platform</p>
                </div>
            </div>

            <div class="flex items-center gap-4">
                <img class="w-16 h-16 object-cover rounded-full border-2 border-white/20" src="/assets/Profile.png" alt="Profile">
                <div class="text-left hidden sm:block">
                    <div class="text-[#e5f1ff] text-[15px] tracking-wide">Authority</div>
                    <div class="text-white font-bold text-xl tracking-wide"><%= name %></div>
                </div>
            </div>
        </div>
    </div>

    <main class="container mx-auto px-6 py-12 max-w-[1400px]">
        
        <div class="mb-14 pl-4 md:pl-10 mt-4">
            <h2 class="text-civic-dark font-bold text-[40px] leading-tight mb-3 font-arial">Welcome to CIVIC-AI !</h2>
            <p class="text-civic-textGray text-xl font-normal leading-relaxed max-w-3xl font-arial">
                Monitor, prioritize and resolve campus issues efficiently. Take informed <br class="hidden md:block" />
                actions to ensure a safe and&nbsp;&nbsp;well- managed campus.
            </p>
        </div>


        <div class="px-4 md:px-8 w-full">
            
            <div class="bg-gray-50 border border-gray-100 p-8 md:p-16 rounded-lg relative min-h-[800px] shadow-sm">
                
                <div class="mb-6">
                    <span class="text-[#101828] text-lg font-arial tracking-wide">Complaint ID : #<%= complaint.id %></span>
                </div>

                <div class="mb-14">
                    <% if (complaint.image_url) { %>
                        <img
                            src="<%= complaint.image_url %>"
                            class="w-full max-h-[400px] object-contain rounded shadow-inner"
                            alt="Complaint Image"
                        />
                    <% } else { %>
                        <div class="w-full h-[400px] bg-[#d9d9d9] rounded shadow-inner flex items-center justify-center text-gray-500">
                            No image uploaded
                        </div>
                    <% } %>

                </div>

                <div class="grid grid-cols-1 md:grid-cols-12 gap-y-12 gap-x-12 mb-16 items-start">
                    
                    <div class="md:col-span-4 flex flex-col gap-3">
                        <h4 class="text-[#101828] text-2xl mb-1 font-arial">Description of the issue</h4>
                        <p class="text-[#4a5565] text-base leading-7 font-arial">
                            <%= complaint.description %>
                        </p>
                    </div>

                    <div class="md:col-span-3 flex flex-col gap-4">
                        <div class="flex items-center gap-3">
                            <h4 class="text-[#101828] text-2xl font-arial">Status:</h4>

                            <span class="text-sm font-medium font-arial pt-1
                                <%= complaint.status === 'registered'
                                    ? 'text-red-600'
                                    : complaint.status === 'in progress'
                                    ? 'text-[#2563eb]'
                                    : 'text-green-600'
                                %>
                            ">
                                <%= complaint.status %>
                            </span>
                        </div>
                        
                        <form method="POST" action="/updateStatus">
                            <input type="hidden" name="id" value="<%= complaint.id %>">
                            <button
                            type="submit"
                            class="text-[13px] font-semibold px-5 py-2.5 rounded
                                flex items-center justify-center w-fit shadow-sm transition-colors

                                <%= complaint.status === 'registered'
                                    ? 'bg-[#dbeafe] text-[#2563eb] hover:bg-blue-200'
                                    : complaint.status === 'in progress'
                                    ? 'bg-[#dcfce7] text-[#16a34a] hover:bg-green-200'
                                    : 'bg-gray-200 text-gray-400 cursor-not-allowed'
                                %>
                            "
                            <%= complaint.status === 'resolved' ? 'disabled' : '' %>
                            >
                            <%= complaint.status === 'registered'
                                ? 'Update status to In Progress'
                                : complaint.status === 'in progress'
                                ? 'Update status to Resolved'
                                : 'Resolved'
                            %>
                            </button>
                        </form>

                    </div>

                    <div class="md:col-span-2 flex flex-col gap-3">
                        <h4 class="text-[#101828] text-2xl mb-1 font-arial">Issue Type</h4>
                        <p class="text-[#4a5565] text-lg font-arial"><%= complaint.issue_type ? complaint.issue_type : 'NULL' %></p>
                    </div>

                    <div class="md:col-span-3 flex flex-col gap-3 pl-0 md:pl-4">
                        <h4 class="text-[#101828] text-2xl mb-1 font-arial">Priority</h4>
                        <div>
                            <% if (!complaint.priority) { %>
                                <span class="text-gray-400 font-semibold text-sm">
                                    NULL
                                </span>
                            <% } else { %>
                                <span
                                    class="px-6 py-2 rounded-full text-sm font-semibold tracking-wide
                                    <%= complaint.priority === 'high'
                                        ? 'bg-[#fee2e2] text-[#ef4444]'
                                        : complaint.priority === 'medium'
                                        ? 'bg-[#dbeafe] text-[#2563eb]'
                                        : 'bg-gray-200 text-gray-700'
                                    %>
                                    "
                                >
                                    <%= complaint.priority.charAt(0).toUpperCase() + complaint.priority.slice(1) %>
                                </span>
                            <% } %>

                        </div>  
                    </div>

                </div>

                <div class="mb-14">
                    <h4 class="text-[#101828] text-2xl mb-5 font-arial">
                        Location
                    </h4>

                    <div
                        class="w-full bg-white border border-[#d0d5db] rounded-[6px] px-5 py-4
                            text-[#474747] text-base font-arial"
                    >
                        <%= complaint.location %>
                    </div>
                </div>


                 <div class="mb-20">
                    <h4 class="text-[#101828] text-2xl mb-5 font-arial">
                        Suggested solution
                    </h4>

                    <div
                        class="w-full bg-white border border-[#d0d5db] rounded-[6px] px-5 py-4
                            text-[#474747] text-base font-arial leading-relaxed"
                    >
                        <%= complaint.suggested_solution %>
                    </div>
                </div>


                <div class="flex justify-center pb-4">
                    <button class="flex items-center gap-3 px-10 py-2.5 bg-white border border-[#d0d5db] rounded-[8px] hover:bg-gray-50 transition shadow-sm group">
                        <img class="w-3 h-3 object-contain group-hover:-translate-x-1 transition-transform" src="https://c.animaapp.com/LNwVnAWI/img/expand-arrow-1@2x.png" alt="Back Arrow">
                        <a href="/authorityDashboard" class="text-civic-dark font-bold text-lg font-arial">back</a>
                    </button>
                </div>

            </div>
        </div>

    </main>

</body>
</html>